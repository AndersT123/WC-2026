[pytest]
# Configuration for session-based testing with persistent database
# Run with: pytest -c pytest_session.ini tests/

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    --tb=short

# asyncio configuration
asyncio_mode = auto
